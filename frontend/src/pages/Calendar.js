import React from 'react';
import Calendar from '../components/Calendar';

const DisplayCalendar = ({hijriDateMap}) => {
  return (
    <div>
      <Calendar />
    </div>
  );
}

export default DisplayCalendar;
